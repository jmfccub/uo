name: blank

on:
  push:

jobs:
  fire:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        preceding: ["neutral", "operation", "assure", "emit", "obviously"]
        mosquito: ["critic", "ad", "guidance", "assign"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar AQ
